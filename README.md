# cXMLValidator
[WIP] A browser-based tool that checks cXML invoice payloads against Coupa’s expected structure before they’re sent — so integration teams can catch problems in seconds instead of hunting them down in failed transmission logs.
